# super-simple-pizza-test-task
Test task in django - to create REST API and test it

# Steps to check
```shell
# createvirtualenv
(venv)$ pip install -r requirements.txt
(venv)$ python3 manage.py test
```
